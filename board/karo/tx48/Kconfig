if TARGET_TX48

config SYS_BOARD
	default "tx48"

config SYS_VENDOR
	default "karo"

config SYS_SOC
	default "am33xx"

config SYS_CONFIG_NAME
	default "tx48"

config TX48
	bool
	default y
	select PHYLIB
	select SPL

endif
